# Handoff: Iknos Phase 1 - Increment 1 (Embedding Substrate)

## Completed
- Cleaned up and committed the leftover unstaged documentation and type fixes from Phase 0 (`82b1ba8`).
- Designed and verified the architecture for the **Embedding Substrate** (§1), explicitly choosing local execution with PyTorch/Transformers to achieve true "late chunking" (embed document once, then pool). This avoids O(L x N) network scaling costs.
- Added `transformers`, `torch`, `pgvector`, and `openai` dependencies (`pyproject.toml` and `uv.lock` updated).
- Implemented the core `EmbeddingSubstrate` and `DocumentContext` in `src/iknos/core/embeddings.py`, resolving character offsets to token offsets for local mean-pooling of the `bge-m3` model.
- Created Alembic migration `0002_add_document_embeddings` and added `DocumentEmbedding` to `src/iknos/db/orm.py` using `VECTOR(1024)`.

## In Progress
- The modifications to `pyproject.toml`, `uv.lock`, `src/iknos/db/orm.py`, `src/iknos/core/embeddings.py`, and the migration file are currently **unstaged**.
- We are ready to begin Increment 2 (Segmentation Backbone), which will wire this new embedding substrate into the dynamic programming chunker.

## Next Steps
- [ ] Commit the current unstaged changes.
- [ ] Implement the **Segmentation Backbone** (Phase 1, Increment 2): adjacent-window similarity boundary detection and DP sentence segmentation.
- [ ] Verify `EmbeddingSubstrate` token-to-char offset logic with a local unit test before wiring it to the chunker.

## Key Files
- `src/iknos/core/embeddings.py` - Core logic for generating token embeddings and performing late-chunking mean-pooling.
- `phase_1_increment_1_plan.md` (Artifact) - The implementation plan detailing the reasoning for local-model late chunking versus remote API.
- `local-llm-setup/README.md` - Hardware reference proving `bge-m3` fits perfectly on the local A2000, while LLM workloads run on the remote 2x3090.

## Context
- **Hardware Routing:** We confirmed that running the embeddings locally on the A2000 12GB is safe and scalable, while heavier tasks (Propositionizer LLM) will be routed to the 192.168.0.247 vLLM instance.
- **Strict Constraint:** Do NOT run `docker compose up` / start any container on this host without explicit per-invocation approval. Because of this, the Alembic migration `0002` was hand-written rather than autogenerated.
