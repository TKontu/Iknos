.venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.env
.env.*
.envrc
!.env.example
!.env.sample
*.log

# Local working handoff note — session-to-session scratch, intentionally not
# tracked (each working copy keeps its own).
HANDOFF.md

# Unrelated material that happens to live in this working tree (other projects;
# contain their own .env files). Not part of Iknos.
factapi_setup/
local-llm-setup/
